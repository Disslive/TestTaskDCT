<Window x:Class="TestTaskDCT.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm ="clr-namespace:TestTaskDCT.ViewModels" 
        xmlns:local="clr-namespace:TestTaskDCT"
        xmlns:oxy="clr-namespace:OxyPlot.Wpf;assembly=OxyPlot.Wpf" 
        mc:Ignorable="d"
        Title="{Binding Title}" Height="450" Width="800">
    <Window.DataContext>
        <vm:MainWindowViewModel/>
    </Window.DataContext>
    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="File">
                <MenuItem Header="Exit" Command="{Binding  CloseApplicationCommand}"></MenuItem>
            </MenuItem>
            <MenuItem Header="Theme">
                <MenuItem Header="{Binding Theme}"></MenuItem>
            </MenuItem>
            <MenuItem Header="Language">
                <MenuItem Header="English"></MenuItem>
                <MenuItem Header="Українська"></MenuItem>
                <MenuItem Header="Русский"></MenuItem>
            </MenuItem>
        </Menu>
        <TabControl>
            <TabItem Header="Main">
                <DataGrid ItemsSource="{Binding BestAssets}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          >
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Rank" Binding="{Binding Rank}"></DataGridTextColumn>
                        <DataGridTextColumn Header="Name" Width="*" Binding="{Binding Name}"></DataGridTextColumn>
                        <DataGridTextColumn Header="Symbol" Width="*" Binding="{Binding Symbol}"></DataGridTextColumn>
                        <DataGridTextColumn Header="Price" Width="*" Binding="{Binding PriceUsd}"></DataGridTextColumn>

                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
            <TabItem Header="Currency Details">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="22*"/>
                        <ColumnDefinition Width="191*"/>
                        <ColumnDefinition Width="573*"/>
                    </Grid.ColumnDefinitions>
                    <ComboBox VerticalAlignment="Center" Width="200" Height="30" Margin="10,10,3,333" Grid.ColumnSpan="2" ItemsSource="{Binding Path=Currencies}" DisplayMemberPath="Name" IsTextSearchEnabled="True"/>
                    <DataGrid ItemsSource="{Binding BestAssets}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True" Grid.ColumnSpan="3" Margin="0,45,0,0"
                          >

                    </DataGrid>
                </Grid>
            </TabItem>
            <TabItem Header="Charts">
                <Grid>
                    <ComboBox VerticalAlignment="Center" Width="200" Height="30" Margin="10,10,576,333" ItemsSource="{Binding Path = Currencies}" DisplayMemberPath="Name" SelectedItem="{Binding PlotCurrency}" IsTextSearchEnabled="True">
                    </ComboBox>
                    <Button Width="200" Margin="245,10,341,333" Height="30" Content="Plot" Command="{Binding DrawChartCommand}"></Button>
                    <oxy:Plot Margin="0,45,0,0">
                        <oxy:Plot.Axes>
                            <oxy:LinearAxis Position="Left"></oxy:LinearAxis>
                            <oxy:DateTimeAxis  IntervalType="Days" MinorIntervalType="Days" Position="Bottom"></oxy:DateTimeAxis>
                        </oxy:Plot.Axes>
                        <oxy:LineSeries Color="Red"
                                        ItemsSource="{Binding Points}"
                                        DataFieldX="Date"
                                        DataFieldY="PriceUSD"/>
                    </oxy:Plot>
                </Grid>
            </TabItem>
            <TabItem Header="Converter">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="161*"/>
                        <ColumnDefinition Width="625*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Height="30" Width="200" Margin="256,58,169,285" Grid.Column="1" Text="{Binding ConvertedCurrency}"></TextBlock>
                    <TextBlock Height="30" Width="200" Margin="100,152,486,191" Text="You Sell" TextAlignment="Center" Grid.ColumnSpan="2"></TextBlock>
                    <TextBox Width="200" Height="30" Margin="100,58,486,285" Grid.ColumnSpan="2" PreviewTextInput="NumberValidationTextBox" Text="{Binding ConvertAmount}"></TextBox>
                    <ComboBox VerticalAlignment="Center" Width="200" Height="30" Margin="100,112,486,231" Grid.ColumnSpan="2" ItemsSource="{Binding Path=Currencies}" DisplayMemberPath="Name" SelectedItem="{Binding ToConvert}" IsTextSearchEnabled="True"></ComboBox>
                    <ComboBox VerticalAlignment="Center" Width="200" Height="30" Margin="256,112,169,231" Grid.Column="1" ItemsSource="{Binding Path=Currencies}" DisplayMemberPath="Name" SelectedItem="{Binding ConvertInto}" IsTextSearchEnabled="True"></ComboBox>
                    <TextBlock Height="30" Width="200" Margin="256,152,169,191" Text="You Get" TextAlignment="Center" Grid.Column="1"/>
                    <Button Width="200" Height="30" Margin="108,187,317,156" Content="Convert" Grid.Column="1" Command="{Binding ConvertCurrencyCommand}"></Button>
                </Grid>
            </TabItem>
            <CheckBox Content="CheckBox" Height="100" Width="100"/>
        </TabControl>
    </DockPanel>
</Window>
